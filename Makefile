
default:	build

clean:
	echo "not ready yet"

build:
	./build.sh

zlib:
	./build.sh zlib

xml:
	./build.sh xml

atomic:
	./build.sh atomic

ssl:
	./build.sh ssl

ssh:
	./build.sh ssh

png:
	./build.sh png

jpg:
	./build.sh jpg

php5:
	./build.sh php

php:
	./build.sh php

nginx:
	./build.sh nginx

mysql:
	./build.sh mysql

ncurses:
	./build.sh ncurses

install:
	echo "not ready yet"

upgrade:
	echo "not ready yet"
