
default:	build

clean:
	echo "not ready yet"

build:
	./build.sh

zlib:
	./build.sh zlib

ssl:
	./build.sh ssl

php:
	./build.sh php

nginx:
	./build.sh nginx

mysql:
	./build.sh mysql

ncurses:
	./build.sh ncurses

install:
	echo "not ready yet"

upgrade:
	echo "not ready yet"
